# 项目任务台账与协作分工

版本：2026-08-05
来源：`接下来方向（一定要全部看完）.md` 的完整核读、当前仓库状态、冻结证据和外场准备文档。

## 1. 项目当前方向

项目当前不以“换一个更大的网络”为目标，而以如下问题为主线：

> 在已有 H/V 双极化雷达数据上，如何提高低慢小目标检测的可信度，重点降低复杂背景下的虚警，同时保持目标检测和距离—速度定位能力？

现阶段证据对象主要是 UAV 与背景，因此当前交付物是检测、定位和虚警抑制前端。空飘球
有载/无载、载荷类型和运动状态是后续数据到位后的任务，不能用当前 UAV 结果代替。

推荐技术链：

```text
数据事实核对
  -> Power2 / DPG-FCN 基线
  -> 逐样本虚警与目标失败分析
  -> H/V 极化特征稳定性审计
  -> 物理特征增强或候选 ROI 复核
  -> 轻量、因果、目标保护的背景校准
  -> 独立外层评价
  -> 新空飘球数据上的分层分类
```

## 2. 我们的分工

### 你的任务：研究负责人和物理事实的把关人

你不需要独自编写全部代码，但必须负责不能由代码自动替代的判断：

- 向学长确认 H/V 通道含义、输入是否为复数 IQ、脉压/FFT/归一化处理、PRF、标签定义和
  Tian 复现条件；
- 逐步查看目标、虚警和困难样本，给出能被雷达物理支持的解释，不能把文件名或猜测当
  成地物类别；
- 决定研究问题、主要指标、停止规则和是否进入下一阶段；
- 维护实验记录：每次正式实验都记录数据清单、分组、配置、代码提交、权重哈希、结果和
  决策；
- 在真实设备或新数据可用时，负责协调采集、同步、标定、真值和场景记录；
- 阅读生成的表格和图，确认结论是否符合你的物理理解，并在论文中承担最终解释责任。

### 我的任务：工程实现、审计和结果整理

我负责把已确定的研究问题变成可运行、可追溯、可拒绝的实验：

- 维护数据接口、特征提取、模型、评价、审计和分享包脚本；
- 先做验证集和训练组上的特征稳定性、来源泄漏、困难组压力检查，再决定是否训练；
- 实现预登记的极化特征增强、局部背景校准和目标保护约束，并保留安全基线；
- 统一输出 pooled、macro、最差折、扫描组不确定性、Pd、Pfa、AUC、定位误差和配对变化；
- 对 Tian 复现做条件核对、输入对齐和失败诊断，不把失败迁移包装成成功复现；
- 维护任务台账、实验台账、论文/分享文档、本地 Git 提交和最新两个分享包；
- 在没有数据或设备证据时明确标记 BLOCKED，不用模拟成功填补缺口。

### 共同任务：决策门

任何新方法进入外层测试前，双方共同确认：

1. 数据范围和组隔离已经写清楚；
2. 主要指标和目标保护约束已经预先登记；
3. 结构、损失、阈值和后处理只由训练/验证数据选择；
4. 失败判据和停止条件已经确定；
5. 测试集只评价一次，结果不反向选择方案。

### 当前方向完成判定

“当前方向完成”不等于项目结束，也不等于可以跳过新数据。它指当前 UAV 检测定位、虚警
机制和后续研究边界已形成可交付闭环。判定由
`configs/current_direction_completion_v1.json` 定义，并用以下命令每次复查：

```bash
python scripts/check_current_direction_completion_v1.py --overwrite
```

只有该检查输出 `COMPLETE` 才提醒负责人“当前方向任务已全部完成”；输出
`BLOCKED_EXTERNAL` 或 `IN_PROGRESS` 时，必须先完成 `remaining_actions.csv` 的对应动作，
不得因为等待外部事实而直接启动新模型训练。

## 3. 待解决问题台账

状态含义：`已完成` 表示当前证据已具备；`进行中` 表示有代码或结果但仍需验收；
`待确认` 表示需要你的外部沟通或物理判断；`阻塞` 表示缺少必要数据/设备；`暂缓` 表示
有价值但当前不进入主线。

| ID | 待解决问题 | 当前状态 | 解决方向 | 负责人 | 完成/放行标准 | 下一动作 |
|---|---|---|---|---|---|---|
| D01 | H/V 到底代表哪种发射/接收通道，数据是复数 IQ 还是已处理 RD | 外部阻塞（当前不可获得） | 已记录材料不可获得；本地只保留 unknown，并关闭需要相干/标定的特征 | 外部数据/设备方提供证据；我维护降级门禁 | 得到可引用的通道、维度、处理链说明；不再用“大概率”写物理结论 | 当前按 bin 域和 relative H/V 推进；仅在出现可归因材料时重开，见 `TIAN_REPRODUCTION_FAILURE_AND_ALTERNATIVES_20260803.md` |
| D02 | PRF、距离门、速度轴和标签坐标是否经过设备方验证 | 外部阻塞（当前不可获得） | 已完成本地字段与采集顺序审计；推定参数不得升级为设备事实 | 外部数据/设备方提供证据；我维护数据卡 | 轴方向、gate/bin 定义和标签误差口径有来源；未确认项进入数据卡限制 | 暂只报告 range gate、Doppler bin 和归一化频率；未来材料到位后重开 |
| D03 | 目标与背景的训练/验证/测试来源是否真正独立 | 部分完成 | 使用 V4 扫描组隔离；补真实采集顺序、日期、场地和 session 元数据 | 我做审计，你联系数据持有人 | causal 和 locked-evaluation 合同通过；目前 33 列缺失，门仍关闭 | 暂不声称外部盲测或因果部署 |
| D04 | 186/187 个历史误警具体是什么物理类型 | 可审计虚警库已完成；完整物理类型仍未知 | 已配对 830 个背景样本及 120→109 个虚警变化；11 例人工记录为 9 例近零多普勒峰、2 例宽结构，物理类别均为 unknown | 我维护本地逐样本库和脱敏聚合层；未来由现场记录补物理标签 | fixed 队列、residual 转移、人工记录和输入哈希一致；分享层不含 sample ID、来源、路径和备注 | 冻结 `ZERO_DOPPLER_FALSE_ALARM_LIBRARY_V1.md`；新同条件现场记录到位后再扩展物理类别复核 |
| D05 | 极化信息是否真的区分目标和虚警 | 部分完成 | 已完成相对特征目录与压力检查；相对极化量只保留为未来 ROI 辅助候选，当前不放行单独检测或绝对极化解释 | 我实现，你解释物理意义 | 新同条件组隔离数据上稳定，通道 validity/标定证据齐全，明确哪些只是 relative / ZDR-like | 等待 D01/D12 与新数据门；当前维持 no-go |
| D06 | 当前高 AUC 是否只是日期、设备或扫描来源泄漏 | 部分完成 | 已报告每背景组 AUC、组依赖和日期混杂，并冻结特征放行门；禁止只报 pooled AUC | 我 | 新数据的最差组和来源压力检查不再只编码日期/文件来源 | 当前把高 AUC 降级为机制诊断，等待同条件外层数据 |
| D07 | 极化增强模型怎样控制复杂度和目标损失 | 进行中 | 先做 Power2、DPG-FCN、ROI 极化和候选 gated encoder 的统一消融；优先小模型和 suppression-only 头 | 我实现，你定主指标 | 在困难折同时满足 Pfa 降低、joint Pd 不下降或达到预登记容差，且无新增虚警 | 只在 D05/D06 通过后训练 |
| D08 | 背景校准怎样做到实时且不泄漏未来信息 | 进行中 | 样本独立 BC 作为在线导向基线；scan-aware 作为离线上限；未来版本按真实时间戳训练 past-only 上下文 | 我实现/审计，你提供顺序信息 | causal 训练使用真实时间，不使用后续样本；窗口只由 train/val 选择 | 当前不从测试敏感性结果选窗口 |
| D09 | 零多普勒虚警抑制是否可泛化 | 进行中 | 固定 soft notch + 目标保护 residual V2 作为开发参考；先验证折间配对变化，再等待外部数据 | 我 | 结构合同保持“不增加任何 logit”；困难折通过后才考虑扩展，外测一次性评价 | 不把 109 个误警写成部署指标 |
| D10 | Tian 2024 FCN 为什么迁移失败 | 外部阻塞（精确复现冻结） | 原迁移、point-GT、负监督和 thesis adapter 失败链已完成；缺原条件时保留方法级负结果 | 外部材料方决定是否能重开；我推进替代路线 | 至少有样例输入/标签/输出、明确配置或同分布数据，才能分离实现差异与域不可辨识性 | 不再扫参；保留 DPG 主线、D17-XBAND 冻结负结果和 Tian 合同测试，见 `TIAN_REPRODUCTION_FAILURE_AND_ALTERNATIVES_20260803.md` |
| D11 | 真实微多普勒和时域特征能否使用 | 部分完成 | LAT-MRICD 的 16,072 条 512 点窄带复数 I/Q 已完成 X 波段归一化频率分组基线和一次预登记跨频段评价；物理微多普勒仍需连续慢时间、真实 PRF、事件起止和状态真值 | 你协调采集，我建接口/公开数据基线 | 公开数据研究按 batch 分组且只报归一化频率；有时间戳、连续 session、状态标签和回放验证后再开放物理 STFT/时频脊线 | 保留 D17-NX 与 D17-XBAND 冻结证据；S/Ku 已消费，不在同一 target 扩模；继续索取 PRF/时序事实 |
| D12 | 极化绝对标定和通道相干是否可信 | 阻塞 | 完成 capability、同步、polarimetric calibration、dry run、pilot 五道门 | 你协调设备证据，我审计模板 | 每道门有证据文件、版本号和重复性记录；没有证据就关闭相位通道 | 使用 `channel_validity` 屏蔽未验证通道 |
| D13 | 新数据能否支持空飘球有载/无载和状态识别 | 阻塞 | 同日目标/背景、无载、有载稳定、有载摆动/旋转、多日期多场地和锁定外层划分 | 你负责采集和真值，我负责合同/模型 | 40 列合同通过；每状态有独立 session 和同步真值；先做分层任务再做细分类 | 当前不报告空飘球分类准确率 |
| D14 | 论文主线如何收敛 | 已完成 | 已冻结为“双极化 UAV 检测定位 + 背景虚警抑制”的当前论文主线，空飘球分类只写未来工作/后续论文 | 共同 | 主表、图件、允许/禁止表述和数据边界已写入 `PAPER_MAINLINE_V1.md` | 新同条件空飘球数据到位后建立 V2，不追溯性扩大旧结论 |
| D15 | 实验是否可长期追溯 | 已完成（当前交付级；后续每轮持续执行） | 当前冻结表格可追溯到证据目录、配置和提交；新实验必须绑定 experiment_id、数据 manifest、split、config、commit、checkpoint 哈希、日志和结果摘要 | 我维护模板，你每轮填写/确认 | 当前分享包达到 A 级证据复核；历史 BC checkpoint 哈希缺口被明确披露，不冒充完整重训练复现；新结果不覆盖旧目录 | 后续每轮按记录协议执行；缺 checkpoint/原始数据时维持对应复现等级 |
| D16 | 分享包和 Git 是否保持可交付 | 已完成（V11） | 只保留 V11 和 V10；原始聊天/数据/权重/逐样本虚警库留本地并忽略；新文档和构建脚本提交本地 Git | 我维护，你确认分享边界 | 包内无敏感路径、原始数据、权重、聊天记录、sample ID 或人工备注；V11 manifest 源提交等于构建时 HEAD；完成门逐项核验证据非空 | 发布新版本时再按“最新+上一版”规则滚动维护 |
| D17 | 外部公开多频段数据能否支撑算法预研 | 已完成（D17-NX/HX/XBAND） | 官方 ZIP、schema/batch 审计和五折 metadata-only 分组均已冻结；X 波段基线与一次性跨频段评价均有最终证据 | 我维护冻结证据和边界；你只按同一公开发布内结果引用 | `results/final_evidence/lat_mricd_grouped_baselines_v1/` 与 `results/final_evidence/lat_mricd_cross_band_transfer_v1/` 可复核；不写成 unseen-model、独立外部、极化、空飘球或 Tian 复现证据 | 不复用已消费 S/Ku target；新公开数据先做独立 schema/group 审计和预登记 |
| D17-XBAND | LAT-MRICD 跨频段迁移是否成立 | 已完成（预登记负结果；`FAIL_STOP`） | LR 的 X->S macro/UAV/weather/CI 下界为 0.6516798767/0.4433201701/0.8600395832/0.0839896354；X->Ku 为 0.8399853939/0.8493090645/0.8306617233/0.2670982858；仅 S UAV recall 失败 | 我维护冻结证据和停止边界；你当前无需提供新数据 | S/Ku target 已消费；`results/final_evidence/lat_mricd_cross_band_transfer_v1/` 可复核；不得在同一 target 上做 CNN、域适配、扩特征、调参或新的确认性比较 | 停止同 target 扩模；如要建立新确认性结论，必须换独立、未消费 target 并重新预登记 |
| D18 | 新公开数据能否形成不泄漏的时域、轨迹和微多普勒算法支线 | 进行中（DAUR、HSR、FMCWR 与 DroneRFc 均完成当前只读审计，但各有外部门禁；未放行建模） | DAUR 77 个逻辑观测仅 76 个唯一 TD/TR 内容对并保守连通为 39 个候选组；HSR V2 冻结 1530 MAT/63148 帧/865 routes 和 529 个隔离 overflow 窗口；FMCWR V4 的 90 MAT 仅 71 个唯一 payload，11 个重复组覆盖 30 文件，66 stems 连通为 48 个非权威候选组，K/L 为 64/26 且 B 通道全空；DroneRFc-MM B1 radar/GT 零重叠 | 我负责来源、loader、schema、合成处理合同和预登记；你判断研究故事；若能联系发布方，优先询问 FMCWR session/Fs/PRF/载频/零频，也可询问 HSR route-source/512-bin 轴、DAUR session/1024 轴或 DroneRFc B1 更正 GT | DAUR、HSR、FMCWR 分别冻结为 `PASS_SCHEMA_PAIRING_BLOCKED_GROUPING_AND_PHYSICAL_AXIS`、`PASS_SCHEMA_BLOCKED_SOURCE_PROVENANCE_AND_PHYSICAL_AXIS`、`PASS_ARCHIVE_SCHEMA_BLOCKED_GROUPING_PROVENANCE_AND_PHYSICAL_AXIS`，均须 `model_training_allowed=false`；每集有版本、许可、哈希、最低分组与禁止声明；FMCWR 归一化轴/单记录处理合同已完成并通过合成 smoke，但不产生物理轴或性能证据 | 维持四个外部阻塞；若发布方补齐 session/Fs/PRF/载频/零频，再为真实记录预登记处理；Ku/NEXRAD 继续 smoke-only，不把 frame/window/屏/gate 当独立性能样本 |

D17-XBAND 的完成以及 D18 的公开数据下载都不替代外部设备事实或 Tian 对齐材料；主 UAV 方向仍为 4/6
`BLOCKED_EXTERNAL`，不得宣布当前方向已全部完成。

## 4. 分阶段计划与双方验收

### 阶段 A：事实与基线（当前优先级 P0）

你的工作：

- 向学长发出一页问题单，优先获取 D01、D02、D10 所需信息；
- 从当前输出中挑选一小批目标、困难背景和虚警样本，逐张看图并记录“可解释/未知”；
- 阅读一份特征定义表，指出哪些物理解释需要降级为相对量。

我的工作：

- 冻结当前 DPG-FCN、sample-independent BC、scan-aware BC-DPG 和 V2 的名称、证据角色和指标；
- 生成虚警/目标配对分析表，避免重新训练和测试集调参；
- 把学长答复接入数据卡和 Tian 复现条件请求。

阶段验收：数据事实清单、当前模型主表、困难样本表和未决问题清单都存在，并且每个结论
带有数据范围、划分方式和证据等级。

### 阶段 B：极化特征挖掘（P0/P1）

你的工作：

- 先确认 H/V 的物理含义和可否进行相位类运算；
- 对 ratio、差异、总功率、归一化能量和相对相关特征给出物理解释；
- 参与决定主验收是“Pfa 降低且 joint Pd 不下降”还是预登记的容差版本。

我的工作：

- 从原始可用通道计算候选特征，区分 Power2、相对极化特征和未经标定的绝对物理量；
- 按扫描组和背景组审计 AUC、PR-AUC、低 FPR 排序、分布稳定性和来源泄漏；
- 只有验证组稳定后，才做轻量特征融合或 ROI 精修模型。

阶段验收：至少一份特征目录、一份跨组压力报告、一份失败/停止记录；如果特征只编码日期
或来源，停止模型化并保留负结果。

### 阶段 C：小幅方法改进（P1）

你的工作：

- 确认方法是否符合研究故事和硬约束；
- 检查困难样本是否真的改善，而不是只看 pooled 数字；
- 在结果评审会上决定继续、降级或停止。

我的工作：

- 实现最小增量模型：极化特征融合、候选 ROI 精修或目标保护残差，只开放一个变量；
- 采用训练/验证选择，保留 epoch 0 和固定安全基线；
- 报告每折、配对样本变化、目标损失和新增虚警。

阶段验收：困难折通过预登记门槛，且方法复杂度、运行时间和解释性有记录；未通过则不扩展
六折，不用测试结果继续调参。

### 阶段 D：因果与外部数据（P0，但当前阻塞）

你的工作：

- 获取真实采集时间、雷达/视频同步、H/V 标定、事件标签和同条件背景；
- 按合同完成 capability、sync、calibration、dry run、pilot 证据；
- 新数据完成后负责场景和真值审核。

我的工作：

- 把真实顺序接入 causal BC 训练和回放；
- 先在训练/验证选择历史窗口，再对外层锁定集一次性评价；
- 接入连续慢时间 STFT、微多普勒、轨迹和分层分类接口。

阶段验收：causal 与 locked-evaluation 两道门打开；在此之前，所有实时/微多普勒/空飘球
结论只能写成待验证方向。

## 5. 明确避雷清单

| 禁止做法 | 为什么危险 | 替代做法 |
|---|---|---|
| 为了涨点盲目换 Transformer、YOLO 或更深 FCN | 没有补充信息，容易把数据泄漏或过拟合误当收益 | 先分析虚警机制和特征稳定性 |
| 只报告 AUC | AUC 不能代表固定阈值下的实际虚警 | 以 Pd、Pfa、worst-fold、joint Pd 和定位误差为主 |
| 用测试结果挑阈值、半宽、联合 AND/OR 或历史窗口 | 把开发结果包装成盲测 | 只用 train/val 选择，外层只评价一次 |
| 把完整扫描上下文称为实时模型 | 可能读取当前样本之后的信息 | 命名为 offline scan-aware upper bound，另训 causal 版本 |
| 把 H/V 相对量称为绝对 ZDR 或物理微多普勒 | 当前标定、PRF、连续慢时间未充分确认 | 使用 relative / ZDR-like，并明确限制 |
| 把 UAV 结果写成空飘球载荷识别 | 类别、平台和标签不一致 | 现阶段写检测定位前端，等待新数据 |
| 直接删除未经核对的历史原始记录 | 破坏决策追溯 | 原始记录本地保留、Git 忽略、分享包脱敏 |
| 重新跑所有已删除的旧训练轮次 | 成本高且不能恢复原实验条件 | 只保存当前冻结证据，新实验严格记账 |
| 复制切片制造“更多独立样本” | 虚增样本量和泛化能力 | 按 session/scan 做独立划分 |
| 在没有设备证据前开放全极化、时序大模型或光学融合 | 数据条件尚不存在 | 先搭接口和门禁，数据到位后再启动 |

## 6. 常态化更新格式

每次推进只更新本文件的状态、证据链接和下一动作，不重新堆叠聊天记录。每条任务至少
补充：

```text
更新日期：YYYY-MM-DD
任务 ID：Dxx
本次动作：
使用的数据/划分：
代码提交：
结果文件或图表：
结论：
是否改变主线：是/否
下一动作与负责人：
```

实验结果另记入 `EXPERIMENT_RECORDING_PROTOCOL.md` 规定的实验台账；本文件只记录任务
状态和决策，不替代逐次实验记录。

## 7. 当前下一步清单

按优先级执行：

1. DAUR 全量只读审计已完成：77 对 TD/TR 与 canonical/backup 等价性通过，但分组键、
   严格时间、日期冲突和 1024-bin 物理轴未过门；不训练、不随机切窗；
2. HSR-L V2 全量只读审计已完成：冻结 1,530 MAT、63,148 帧、865 routes 和官方
   45,366/9,336 个 train/validation 窗口；529 个 overflow 窗口隔离。route 是最低分组，
   source-session/场景来源与 DPL 物理轴未确认，状态为
   `PASS_SCHEMA_BLOCKED_SOURCE_PROVENANCE_AND_PHYSICAL_AXIS`，`model_training_allowed=false`；V2 `Dataset.py` 只读，
   期刊历史包的移动原件旧脚本不得运行，两包不混用；
3. FMCWR-2.0 全量只读审计已完成：6 个 RAR、90 MAT、64 K/26 L、71 个唯一 payload、
   11 个精确重复组/30 个成员、66 stems 和 48 个非权威候选组已冻结；B 通道全空，不能称
   H/V。状态为 `PASS_ARCHIVE_SCHEMA_BLOCKED_GROUPING_PROVENANCE_AND_PHYSICAL_AXIS`，
   `model_training_allowed=false`；归一化轴合成/单记录处理合同和合成 smoke 已完成，只输出
   index/cycles-per-sample 轴，不生成性能。继续等待发布方确认 session、Fs/PRF、载频和零频
   后才决定真实记录的预登记处理；仿真飞鸟始终单独标记；
4. DroneRFc-MM 已完成 9 个 PCD recording 的全量只读 schema/时间覆盖审计：8 条时间范围
   重叠，B1 为零重叠并冻结 `BLOCKED`；只有取得更正 GT/可归因偏移后才重开 B1；
5. Ku 群目标和 NEXRAD 已完成低成本格式核验，只保留航迹/双极化接口 smoke，不开放识别
   训练；42.4/23.46/30.36 GB 三个主包仅登记，出现明确算法缺口前不下载；
6. 你当前只需阅读各数据审计结论，判断它们是否服务“低空目标多域特征”研究故事；不需要
   继续手工下载，也不需要为公开数据补标签；
7. Tian 精确复现继续冻结；只有学长提供可归因的对齐样例、完整配置或同分布数据时才重开；
8. 2026 年 8--9 月仍需确认雷达极化和连续采集能力，完成 SOP、同步、标定、dry run 与
   pilot，为空飘球正式数据做准备。

当前最重要的个人任务不是“再跑一个模型”，而是拿到数据事实、理解困难样本、参与指标
和停止规则的决定。当前最重要的工程任务不是“再写一个网络”，而是让每个结论都能追溯、
能复核、能被拒绝。
