# 下一阶段研究与数据计划 V1

版本：2026-08-03

## 1. 采用外部建议后的判断

采纳的主线是：数据可信性 -> 虚警机制 -> 极化特征 -> 最小模型增量 -> 分组与外层验证。
当前不缺一个更大的网络，缺的是 H/V 和坐标事实、误警物理证据、同条件数据以及未消费的
外层评价。

附件中的建议与当前仓库相比，需要做三项校正：

1. 当前成果表、56 项多域特征目录和初步极化压力检查已经完成，不重复建立同类结果；
2. 历史 186/187 个误警可以先按可见结构审计，但建筑、鸟、地物等物理类别必须有独立
   场景记录，不能从文件名或 RD 图猜测；
3. `Polarization-aware BC-DPG-FCN` 是数据门通过后的候选，不在目标/背景日期完全混杂的
   六折上继续训练或选结构。

当前论文问题保持为：在 H/V 双通道 UAV/背景数据上，建立可追溯的检测、距离-速度定位和
复杂背景虚警抑制前端。空飘球载荷识别是新数据到位后的下一研究对象。

## 2. 下一步任务表

| 顺序 | 任务 | 当前状态 | 我的工作 | 你的工作 | 完成/放行标准 |
|---:|---|---|---|---|---|
| 1 | Fold 4 的 11 个 P0 零多普勒误警复核 | 已完成 | 已完成 CSV 审计和 D04 回填；保留不含样本 ID 的聚合结论 | 11 例已填写可见模式和备注，物理类别均保持 `unknown` | 11/11 通过审计：9 例近零多普勒峰、2 例宽结构 |
| 2 | H/V、IQ、PRF 和坐标事实确认 | 外部阻塞；当前报告不可获得 | 按 unknown 门禁维护数据卡，只开放 bin 域和 relative H/V 分析 | 当前不要求继续追索；未来有可归因材料时转交核验 | 有来源才解锁物理坐标/相干特征；否则长期保持 unknown |
| 3 | Tian 2024 最小条件对齐 | 外部阻塞；精确复现冻结 | 保留方法级实现和负结果，推进 DPG/LAT-MRICD/合同测试替代路线 | 可把失败说明发给学长征求方向判断，不承担猜测原条件 | 只有获得对齐样例、完整配置或同分布数据之一才重开训练 |
| 3A | LAT-MRICD 公开数据算法预研 | 数据审计完成，待冻结分组 | 按 batch 分组建立 Narrow-X/HRRP-X 可解释特征基线，再做跨频段迁移 | 确认是否把该公开数据作为队员暑期复现支线 | 禁止随机拆行；先报 batch 级 macro/最差组；无 PRF 时只报归一化频率 |
| 4 | 建立可审计的虚警库 | P0 复核后开始 | 把 fixed/residual、特征、可见模式和证据来源合并，按 scan 汇总 | 优先核对有现场记录的误警事件，不确定项不强行分类 | 每个命名物理类别可追溯；同时保留 unknown 比例 |
| 5 | 新同条件数据的极化统计 | 等待数据 | 冻结特征、按 session/scan 做 AUC/PR-AUC/低 FPR/最差组压力检查 | 审核特征物理解释与主验收指标 | 来源压力通过；目标/背景不再由日期直接区分 |
| 6 | 最小极化增强模型 | 未放行 | 先做功率/相对幅度 ROI 小分支，一次只开放一个变量 | 评审困难样本、复杂度和停止规则 | 验证组 Pfa 降低且 joint Pd 不下降，或通过预登记容差 |
| 7 | 因果背景校准 | 等待真实顺序 | 用 past-only 窗口训练，训练/验证选窗口，锁定外层只评一次 | 提供硬件时间戳、序号和丢帧记录 | 不读取未来样本，causal 合同通过 |
| 8 | 空飘球载荷/状态迁移 | 等待新任务数据 | 接入分层标签、时域/微多普勒和极化接口 | 组织无载/有载、状态和同步真值采集 | 每类有独立 session，locked test 与开发组隔离 |

只有完成度检查器输出 `COMPLETE`，才宣布当前方向任务全部完成。当前仍是
`BLOCKED_EXTERNAL`，不能用新增模型代替第 1 至 3 项。

2026-08-03 补充决策：项目负责人报告第 2、3 项所需外部材料目前无法取得。两项转为
“已记录的外部阻塞”，不再要求通过猜值或重复扫参解决；底层事实仍为 unknown，完成门禁
不会被替代模型伪装解除。详细失败链、替代路线和重开条件见
`TIAN_REPRODUCTION_FAILURE_AND_ALTERNATIVES_20260803.md`。

## 3. 现在最需要的数据和材料

### 3.1 第一优先级：不需要传大数据，先补事实

请优先提供或向负责人确认：

| 材料 | 最小可接受形式 | 用途 |
|---|---|---|
| H/V 通道定义 | 设备说明、配置截图或可归因文字答复 | 决定 H/V 是极化通道、接收通道还是切换结果 |
| IQ 处理链 | 变量定义、FFT/窗/归一化说明或最小处理脚本 | 确定现有 MAT 是原始 IQ 还是处理后数据 |
| PRF 与帧结构 | PRF、每帧脉冲数、距离门、帧/波束/扫描/文件关系 | 建立真实速度轴和时域可用性 |
| 坐标与标签 | 距离/速度到 bin 公式、索引起点、正负号和标注方法 | 核对定位指标和 Tian 标签 |
| H/V 同步与标定 | 同时/交替采样说明、幅相标定方法和 calibration ID | 决定能否开放相位、相关和极化差特征 |
| 11 例 P0 人工结果 | 工作台导出的 CSV | 建立第一批可审计虚警记录 |

对应可转发文字已经在 `EXTERNAL_FACT_REQUEST_MESSAGE_V1.md`。只拿到“不知道/无法提供”也
有价值，因为它能把通道明确关掉，避免错误物理结论。

### 3.2 极化小模型所需的最小新数据

这批数据用于回答“极化信息是否在同条件下帮助区分目标和背景”，必须具备：

- 同一日期、场地、雷达配置和标定状态下同时采目标与无目标背景；
- H/V 成对数据；优先原始复数 IQ，至少保留未分别自适应归一化的线性功率；
- `session_id`、`scan_id`、硬件时间戳/序号、目标存在、距离、径向速度和背景类型；
- 每个通道的有效标志、标定编号、异常/丢帧记录；
- 多个独立 session，按 session 或日期分开发、验证和 locked test，不能切片后随机混分；
- 背景物理类别若要命名，必须有现场记录、视频或日志，允许大量样本标 `unknown`。

先做小规模 pilot 检查格式、同步、类间同条件对照和特征稳定性；pilot 通过后再扩数量。当前
不预先承诺一个样本条数，因为独立 session 数、场景覆盖和外层隔离比从同一记录切出更多帧
更重要。

### 3.3 时域、因果 BC 和微多普勒额外需要的数据

- 连续逐脉冲慢时间复数 IQ，而不是互不连续的单帧 RD 图片；
- 真实 PRF、脉冲序号、帧序号、UTC 时间戳、时钟重置和丢帧日志；
- 每个事件的开始/结束、连续时长及雷达配置；
- 雷达与视频/运动真值的同步关系和误差；
- 固定正式配置下至少 300 秒连续写盘能力测试，用于验证吞吐和连续性；
- 多个独立连续事件，开发、验证和锁定测试按 session 隔离。

这些材料到位后，才能计算有物理时间/频率单位的 STFT、时频脊线、周期和微多普勒特征；
现有 128 脉冲单帧只允许结构描述，不能支持物理微多普勒结论。

### 3.4 空飘球载荷与状态识别额外需要的数据

- `balloon_unloaded`、`balloon_loaded`，以及明确的 `payload_class`；
- 稳定、摆动、旋转等 `motion_state`，并由同步视频或传感器记录确认；
- 多个球体、多个载荷实例、多个日期/场地和重复独立放飞 session；
- 同条件 UAV、鸟类、其他目标和无目标背景，支持检测前端的混淆分析；
- 距离、径向速度、方位、SNR、天气和风况；
- 类别与 session 不得一一绑定，locked test 中需要未见过的 session/日期或实体。

建模顺序应是：目标/背景检测 -> 空飘球/其他目标 -> 有载/无载 -> 载荷或运动状态细分，
不直接用当前 UAV 六折训练一个“空飘球识别器”。

### 3.5 Tian 复现所需材料

优先级从高到低：

1. 原代码或 checkpoint 加一个输入、标签、输出对齐样例；
2. 完整的预处理、轴顺序、5x7 GT、负样本、损失和训练超参数；
3. 覆盖多个距离/速度输出网格且具有同条件背景的同分布数据；
4. 学长期望复现的指标、容差和数据划分。

只要取得其中一类足以解除门禁的材料，就先做一个样本和一个小 split 的对齐，不直接跑
六折。

### 3.6 已取得的公开数据与仍缺的字段

LAT-MRICD-1.0 已取得 7,119 条 HRRP 和 16,072 条窄带 I/Q，可直接支持 HRRP、归一化
频率谱和 UAV/鸟/气象分组基线。它没有 H/V、空飘球标签，也未确认 PRF、逐脉冲时间戳、
连续 session 和同事件跨频段配对。因此它减少了算法接口等待，但没有替代 3.1 至 3.5 的
设备事实和任务数据。完整审计见 `EXTERNAL_PUBLIC_DATA_AUDIT_20260803.md`。

## 4. 极化特征的使用边界

| 候选量 | 可用条件 | 当前命名 |
|---|---|---|
| `|H|^2 + |V|^2` | H/V 均有效，线性量纲一致 | 双通道总功率 |
| `P_H / (P_V + eps)` 或 dB 比 | 幅度响应至少可比较；未绝对标定 | relative H/V ratio、ZDR-like |
| `P_H / (P_H + P_V + eps)` | H/V 同步且尺度可比 | 相对归一化功率 |
| `|H - V|`、复相关、相位差 | H/V 同步、相干、相位对齐且处理链明确 | 门通过后再定义 |
| 极化一致性 loss | 有可信监督或预登记的自监督物理约束 | 当前不启用 |

因此第一版候选不是直接上极化注意力，而是先验证总功率、相对功率比和 ROI 统计。简单分支
在新验证组稳定后，才比较 gated fusion/attention；物理约束 loss 必须最后考虑。

## 5. 交付与停止规则

1. 新数据先填 `configs/data_collection_manifest_template_v1.csv`，再运行
   `validate_data_collection_manifest.py`；原始大文件不进 Git 或分享包。
2. 特征定义、归一化拟合范围、阈值、主指标和停止规则在读取 locked test 前冻结。
3. 主指标至少包含 Pfa、joint Pd、最差背景组、定位误差和配对新增/移除误警；AUC 只作补充。
4. 如果特征主要编码日期、文件来源或扫描组，停止模型化并保留负结果。
5. 如果困难验证组未通过，停止六折扩展；不靠更多 epoch、更多通道或 test 调参挽救。
6. complete-scan BC 始终标为离线扫描感知上限；只有真实 past-only 顺序可称因果方案。

## 6. 立即执行清单

你的三个动作：

1. 打开 `results/data_audit/zero_doppler_review_atlas_v1/review_workbench.html`，完成 11 例 P0；
2. 发送 `EXTERNAL_FACT_REQUEST_MESSAGE_V1.md` 中的两段消息，保存原始答复和附件；
3. 告诉我目前最可能拿到的是“设备说明/配置”“一个最小 IQ 样例”“一批新采数据”中的哪类。

我的三个动作：

1. 完成本地工作台、测试、Git 提交和 2026-08-03 分享包；
2. 收到复核 CSV 后运行审计，更新 D04 和完成度门；
3. 收到任何设备/Tian/新数据材料后，先做合同与来源核验，再决定开放哪个算法分支。
